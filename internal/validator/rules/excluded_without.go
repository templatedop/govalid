// Code generated by generate-validators; DO NOT EDIT.
package rules

import (
	"fmt"
	"go/ast"
	"strings"

	"github.com/gostaticanalysis/codegen"

	"github.com/sivchari/govalid/internal/validator"
	"github.com/sivchari/govalid/internal/validator/registry"
)

type excluded_withoutValidator struct {
	pass       *codegen.Pass
	field      *ast.Field
	structName string
	ruleName   string
	parentPath string
}

var _ validator.Validator = (*excluded_withoutValidator)(nil)

const excluded_withoutKey = "%s-excluded_without"

func (v *excluded_withoutValidator) Validate() string {
	// TODO: Implement validation logic
	// Return a Go expression that evaluates to true when validation fails
    return ""
}

func (v *excluded_withoutValidator) FieldName() string {
	return v.field.Names[0].Name
}

func (v *excluded_withoutValidator) FieldPath() validator.FieldPath {
	return validator.NewFieldPath(v.structName, v.parentPath, v.FieldName())
}

func (v *excluded_withoutValidator) Err() string {
	key := fmt.Sprintf(excluded_withoutKey, v.FieldPath().CleanedPath())

	if validator.GeneratorMemory[key] {
		return ""
	}

	validator.GeneratorMemory[key] = true

  const deprecationNoticeTemplate = `
		// Deprecated: Use [@ERRVARIABLE]
		//
		// [@LEGACYERRVAR] is deprecated and is kept for compatibility purpose.
		[@LEGACYERRVAR] = [@ERRVARIABLE]
  `

	const errTemplate = `
	  // [@ERRVARIABLE] is returned when the [@FIELD] fails excluded_without validation.
	  [@ERRVARIABLE] = govaliderrors.ValidationError{Reason:"field [@FIELD] failed excluded_without validation",Path:"[@PATH]",Type:"[@TYPE]"}
	`

  legacyErrVarName := fmt.Sprintf("Err%s%sExcluded_withoutValidation", v.structName, v.FieldName())
	currentErrVarName := v.ErrVariable()

	replacer := strings.NewReplacer(
        "[@ERRVARIABLE]", currentErrVarName,
		"[@LEGACYERRVAR]", legacyErrVarName,
		"[@FIELD]", v.FieldName(),
		"[@PATH]", v.FieldPath().String(),
		"[@TYPE]", v.ruleName,
	)

  if currentErrVarName != legacyErrVarName {
		return replacer.Replace(deprecationNoticeTemplate + errTemplate)
	}

	return replacer.Replace(errTemplate)
}

func (v *excluded_withoutValidator) ErrVariable() string {
	return strings.ReplaceAll("Err[@PATH]Excluded_withoutValidation", "[@PATH]", v.FieldPath().CleanedPath())
}

func (v *excluded_withoutValidator) Imports() []string {
	// TODO: Add any required imports
	return []string{}
}

// ValidateExcluded_without creates a new excluded_without validator for the given field.
func ValidateExcluded_without(input registry.ValidatorInput) validator.Validator {
	return &excluded_withoutValidator{
		pass:       input.Pass,
		field:      input.Field,
		structName: input.StructName,
		ruleName:   input.RuleName,
		parentPath: input.ParentPath,
	}
}
